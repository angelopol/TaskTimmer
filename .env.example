## Copy this file to .env and fill real values

#############################################
# Database
#############################################
# Project supports SQLite (default for dev) or MySQL.
# Select provider via DB_PROVIDER=sqlite|mysql
# SQLite is great for quick local development.
# MySQL recommended for staging/production.

DB_PROVIDER="sqlite"

# Option 1: SQLite (when DB_PROVIDER=sqlite)
DATABASE_URL="file:./prisma/dev.db"

# Option 2: MySQL (uncomment & adjust when DB_PROVIDER=mysql)
# MYSQL_HOST="localhost"
# MYSQL_PORT="3306"
# MYSQL_USER="root"
# MYSQL_PASSWORD="password"
# MYSQL_DATABASE="tasktimmer"
# DATABASE_URL="mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}"

## Secret to sign NextAuth JWT (generate: openssl rand -base64 32 )
NEXTAUTH_SECRET="change_this_value"

## Public app URL (for callbacks, locally usually http://localhost:3000)
NEXTAUTH_URL="http://localhost:3000"

## (Optional) Future settings
# LOG_LEVEL=info

## Session duration configuration
# Short session (hours) when user does NOT check Remember me
SHORT_SESSION_HOURS=6
# Long session (days) when Remember me IS checked
LONG_SESSION_DAYS=30

DEMO_SEED=false

NODE_ENV=production