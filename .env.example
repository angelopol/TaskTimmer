# Copia este archivo a .env y rellena valores reales

#############################################
# Base de Datos
#############################################
# El proyecto soporta SQLite (dev por defecto) o MySQL.
# Selecciona el proveedor con DB_PROVIDER=sqlite|mysql
# SQLite es ideal para desarrollo local rápido.
# MySQL recomendado para staging/producción.

DB_PROVIDER="sqlite"

# Opción 1: SQLite (usar cuando DB_PROVIDER=sqlite)
DATABASE_URL="file:./prisma/dev.db"

# Opción 2: MySQL (descomenta y ajusta cuando DB_PROVIDER=mysql)
# MYSQL_HOST="localhost"
# MYSQL_PORT="3306"
# MYSQL_USER="root"
# MYSQL_PASSWORD="password"
# MYSQL_DATABASE="tasktimmer"
# DATABASE_URL="mysql://${MYSQL_USER}:${MYSQL_PASSWORD}@${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}"

# Secret para firmar JWT de NextAuth (genera uno con: openssl rand -base64 32 )
NEXTAUTH_SECRET="cambia_este_valor"

# URL pública de la app (para callbacks, en local normalmente http://localhost:3000)
NEXTAUTH_URL="http://localhost:3000"

# (Opcional) Ajustes futuros
# LOG_LEVEL=info
