# Copia este archivo a .env y rellena valores reales

# URL de la base de datos (SQLite local)
DATABASE_URL="file:./prisma/dev.db"

# Secret para firmar JWT de NextAuth (genera uno con: openssl rand -base64 32 )
NEXTAUTH_SECRET="cambia_este_valor"

# URL pública de la app (para callbacks, en local normalmente http://localhost:3000)
NEXTAUTH_URL="http://localhost:3000"

# (Opcional) Ajustes futuros
# LOG_LEVEL=info
